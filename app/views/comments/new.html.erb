<section class="new-comment-container">
  <%= form_with model: @comment, url: user_post_comments_path, local: true do |form| %>
    <%= form.text_field :text, class: "comment-field", placeholder: "Enter a text here" %>
    <%= form.submit "Add comment", class: "submit-btn btn" %>
  <% end %>
</section>

